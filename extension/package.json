{
  "name": "deepdive-assistant-extension",
  "version": "0.1.0",
  "description": "Chrome Extension for intelligent article analysis",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:prod": "NODE_ENV=production node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
